{
  "name": "supernova-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Public read-only SuperNova MCP connector prototype.",
  "scripts": {
    "dev": "node scripts/local-server.mjs",
    "build": "node --check src/mcpServer.js && node --check api/mcp.js && node --check api/health.js && node --check scripts/local-server.mjs && node --check scripts/smoke-mcp.mjs",
    "test": "node --test",
    "smoke": "node scripts/smoke-mcp.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "zod": "^3.25.0"
  },
  "engines": {
    "node": ">=20"
  }
}
